# ElectionsDataset_API
This project converts historical Pakistan Elections API data (2008, 2013, 2018, 2024) into a clean dataset and exposes it through a Flask API. Includes endpoints for province-wise and year-wise results. The dataset is generated dynamically using the provided Python script.
